/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PolicyCreateSelectorComponent } from './policy-create-selector.component';

describe('PolicyCreateSelectorComponent', () => {
  let component: PolicyCreateSelectorComponent;
  let fixture: ComponentFixture<PolicyCreateSelectorComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [PolicyCreateSelectorComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(PolicyCreateSelectorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/

xdescribe('policy-create-selector.component legacy placeholder', () => {
  it('skipped', () => {});
});
